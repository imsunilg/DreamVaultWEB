export const environment = {
  production: false,
  apiUrl: 'http://localhost:5895/api',
  // Development-only: shows the demo Admin/User credentials on the login page.
  // Set to false (see environment.production.ts) before deploying anywhere real.
  showDemoCredentials: true
};
